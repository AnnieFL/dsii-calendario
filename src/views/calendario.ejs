<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css">
    <title>Dsii-Calendario</title>
</head>

<body class="containerInicio">
    <h1 class="center">Calendario de <%=userPrincipal.nome%></h1>
    <h2 class="center"><%=mes%></h2>
        <div class="containerCalendario">
            <table class="calendario">
                <tr>
                    <th>Dom</th>
                    <th>Seg</th>
                    <th>Ter</th>
                    <th>Qua</th>
                    <th>Qui</th>
                    <th>Sex</th>
                    <th>Sab</th>
                </tr>
                <%for (let i=0; i<dias.length; i++) {%>
                    <%if (i%7==0) {%>
                        <tr>
                    <%}%>
                    <td class="<%=dias[i].class%><%if (dias[i].evento) {%>" style="background-color:<%=dias[i].evento.cor%>"<%}%>">
                            <%if (dias[i].evento) {%><a class='nonlink' href="/evento/<%=dias[i].evento.dataValues.id%>"><%} else {%>
                                <a class='nonlink' href="/evento/0?data=<%=`${anoView}`%>-<%if (mesView.length > 1) {%><%= mesView %><%} else {%><%=`0${mesView}`%>-<%}%><% if (dias[i].dia.length > 1) {%><%=dias[i].dia%><%} else {%><%=`0${dias[i].dia}`%><%}%>"><%}%>
                            <%=dias[i].dia%></a>
                        </td>
                    <%if (i+1%7 == 0) {%>
                        </tr>    
                    <%}%>
                <%}%>
            </table>
        </div>
        <div class="outrosMeses">
            <a href="/calendario/<%=idParam%>?mes=<%=(parseInt(mesQuery)-1)%>">< Mês anterior</a>
            <a href="/calendario/<%=idParam%>?mes=<%=(parseInt(mesQuery)+1)%>">Próximo mês ></a>
        </div>
</body>

</html>